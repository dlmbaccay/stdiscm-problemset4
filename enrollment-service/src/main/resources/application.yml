spring:
  application:
    name: enrollment-service

  datasource:
    url: jdbc:postgresql://db.btflszgdqciqgsaodwra.supabase.co:5432/postgres
    username: postgres
    password: GcaoWp3yZlGKmVZA
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka  # Eureka server URL

server:
  port: 8084  # Port for Auth Service